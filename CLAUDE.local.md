# Personal Overrides — OnlyMock (not committed to git)

## My Preferences

- Prefer functional components with hooks — never class components
- When showing code, show the full file — not just the changed section
- After every implementation, remind me to run `npm run typecheck`

## Local Dev Notes

- Local DB: PostgreSQL running on localhost:5432
- Use `npm run db:studio` to inspect data visually
- Razorpay test keys in .env.local — never commit
